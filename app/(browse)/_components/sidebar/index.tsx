import { ToggleSidebar } from "./toggle-sidebar";
import { Wrapper } from "./wrapper";

export const SideBar = () => {
  // fetch followed
  // fetch recommended
  return (
    <Wrapper>
      <ToggleSidebar />
    </Wrapper>
  );
};
